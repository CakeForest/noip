#include<stdio.h>
#include<string.h>
#include<algorithm>
#include<math.h>
#include<stdlib.h>
using namespace std;
int a[20],z[20];
struct node{
	int x,y,dis;
};
node f[20];
int main()
{
	freopen("wine.in","r",stdin);
	freopen("wine.out","w",stdout);
/*	int n,m;
	scanf("%d%d",&n,&m);
	for(int i=1;i<=n;i++)
		scanf("%d",&a[i]);
	for(int i=1;i<=n;i++){
		if(a[i]==0)z[++t]=i;
	}
	int x,y;
	for(int i=1;i<=m;+=i++)
		scanf("%d%d%d",&f[i].x,&f[i].y,&f[i].dis);
	int t=0;
*/	
	printf("Impossible");
	return 0;
}
